# Base URL of the Flask REST API.
VITE_API_BASE_URL=http://localhost:5000/api

# false = talk to the real Flask + MySQL backend.
# true  = use the in-browser mock, no backend needed.
VITE_USE_MOCK=false
