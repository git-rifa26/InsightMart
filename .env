# Base URL of the Flask REST API
VITE_API_BASE_URL=http://localhost:5000/api

# When true the app is served by the in-browser mock backend.
# Set to false once the Flask backend is running.
VITE_USE_MOCK=true
